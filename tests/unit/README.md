# Unit Tests

Small contract tests for one function or class at a time.

Keep these tests cheap and local. Prefer fake or minimal collaborators, pytest
temporary directories, and deterministic CPU calculators. Do not invoke the
`nequip-distill` CLI or run NequIP training here.
