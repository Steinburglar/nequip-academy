# Sample Integration Tests

Integration tests for generator behavior across filesystem state, ASE IO, and
resume bookkeeping.

The existing resume tests live here because they verify full dataset/resume
behavior rather than isolated helper contracts.
