# Sample Unit Tests

Tests for `nequip_extension_template.sample`.

Expected files here include:

- `test_split.py` for `assign_splits` behavior.
- `test_sampler.py` for base `Sampler` filesystem and state-record behavior.
- `test_rattle.py` for `RattleSampler` ordering, seeding, labeling, and resume
  position behavior.
- `test_md.py` for `MDSampler` trajectory sampling and its current resume refusal.
