# Script Unit Tests

Small tests for helpers in `nequip_academy.scripts`.

This is the right place for run-list parsing, train-config preflight checks, and
config rewrite behavior that can be tested without launching a subprocess.
