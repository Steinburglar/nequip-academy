# Sample Unit Tests

Tests for `nequip_extension_template.sample`.

Expected files here include:

- `test_split.py` for `assign_splits` behavior.
- `test_generator.py` for base `Generator` filesystem and state-record behavior.
- `test_rattle.py` for `RattleGenerator` ordering, seeding, labeling, and resume
  position behavior.
- `test_md.py` for `MDGenerator` trajectory sampling and its current resume refusal.
