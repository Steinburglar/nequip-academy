# Integration Tests

Tests for multiple project components working together.

These may use ASE IO, generator state files, generated datasets, and pytest temp
directories. Keep them CPU-only unless a test is explicitly marked otherwise.
